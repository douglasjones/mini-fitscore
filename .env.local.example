# Variáveis de ambiente para configuração do Firebase
# Copie este arquivo para .env.local e preencha com seus próprios valores.
# NUNCA adicione o arquivo .env.local ao controle de versão (Git).

# O ID do seu aplicativo, usado no caminho do Firestore.
NEXT_PUBLIC_APP_ID="default-app-id"

# A configuração do seu projeto Firebase.
# Você pode obter isso no console do Firebase, nas configurações do seu projeto web.
# O valor deve ser uma string JSON, por exemplo:
# NEXT_PUBLIC_FIREBASE_CONFIG='{"apiKey": "AIza...", "authDomain": "...", ...}'
NEXT_PUBLIC_FIREBASE_CONFIG='{}'

# (Opcional) Token de autenticação inicial, se necessário.
# NEXT_PUBLIC_INITIAL_AUTH_TOKEN=""
